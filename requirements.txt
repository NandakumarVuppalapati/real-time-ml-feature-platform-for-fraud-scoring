# Full dev environment — installs everything needed to run every piece of
# this project directly on your machine (no Docker), for learning/debugging.
# Individual services use their own slim requirements.txt (ingestion/,
# streaming/, training/, serving/) when built as Docker images, so each
# container only carries what it actually needs.

feast==0.65.0
mlflow==3.14.0
pyspark==3.5.1
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
kafka-python>=2.0,<3.0
redis>=5.0,<6.0
pandas>=2.0,<3.0
pyarrow>=14.0,<19.0
pytest>=8.0,<9.0
httpx>=0.27,<1.0
